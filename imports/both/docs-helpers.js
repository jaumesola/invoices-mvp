
/*
 
 NOT REQUIRED ANY MODE TODO remove soon
 
// returns an object with a subset of fields (properties) for the database
export const docFromObject = function docFromObject(object, fields) {
	var doc = {};
	fields.forEach( x => { doc[x] = object[x] });
	return doc;
}

// returns an Astronomy object with a subset of fields (properties) for the database

export const astrodocFromObject = function astrodocFromObject(from, to, fields) {
	fields.forEach( x => { to[x] = from[x] });
	return to;
}

*/